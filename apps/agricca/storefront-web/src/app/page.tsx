// Phase 0 placeholder page. storefront-web is a thin host:client shell: it may only
// reach shared client libs (sdk / ui), never the server engine. No real UI yet.
export default function Index() {
  return (
    <main>
      <h1>Agricca</h1>
      <p>Storefront shell — Phase 0.</p>
    </main>
  );
}
