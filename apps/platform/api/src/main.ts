// Phase 0 placeholder. platform-api is the shared REFERENCE / baseline API — what an
// OpCo with no server customization deploys directly (it is NOT the sole deployable).
// A thin shell here: the NestJS host, modules, routes and DB wiring are a later slice.
// This only proves the app builds and boots under the boundary rules.
process.stdout.write('platform-api: Phase 0 shell\n');
