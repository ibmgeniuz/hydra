// Phase 0 placeholder — see this project's README. The control plane (per-OpCo
// entitlement/config serving + the license kill-switch) is built AFTER Phase 1. It
// carries host:server as a temporary simplification and will split into a server +
// client project then. Inert, tagged shell for now.
process.stdout.write(
  'platform-control-plane: Phase 0 shell (build after Phase 1)\n',
);
