// Phase 0 placeholder. refleek-core is an empty, tagged shell — it exists so the
// provenance/host boundary rules have a real project to enforce against. Refleek's
// OpCo server leaf; here only to test the opco<->opco boundary.
export const refleekCore = 'refleek-core' as const;
