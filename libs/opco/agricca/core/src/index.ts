// Phase 0 placeholder. agricca-core is Agricca's OpCo-specific server leaf — the
// snippable IP that only apps/agricca/* may compose. Empty tagged shell for now.
export const agriccaCore = 'agricca-core' as const;
