// Phase 0 placeholder. orders is an empty, tagged shell — it exists so the
// provenance/host boundary rules have a real project to enforce against. Order lifecycle / state-machine logic lands in a later slice.
export const orders = 'orders' as const;
