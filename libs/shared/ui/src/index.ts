// Phase 0 placeholder. ui is an empty, tagged shell — it exists so the
// provenance/host boundary rules have a real project to enforce against. Shared client UI primitives land in a later slice (host:client).
export const ui = 'ui' as const;
