// Phase 0 placeholder. entitlements is an empty, tagged shell — it exists so the
// provenance/host boundary rules have a real project to enforce against. Licensing entitlements + control-plane wiring land in Phase 1.
export const entitlements = 'entitlements' as const;
