// Phase 0 placeholder. sdk is an empty, tagged shell — it exists so the
// provenance/host boundary rules have a real project to enforce against. Reserved for the Orval-generated typed client (host:client).
export const sdk = 'sdk' as const;
