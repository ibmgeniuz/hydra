// Phase 0 placeholder. financial-engine is an empty, tagged shell that exists to
// exercise the provenance/host boundary rules. Money types, commission, escrow and
// payout logic land in a later, logic-bearing slice — not here.
export const financialEngine = 'financial-engine' as const;
